return {
    "SidOfc/mkdx",
    ft = "markdown",
    config = function()
        vim.g["mkdx#settings"] = {
            map = {
                prefix = "<localleader>"
            }
        }
    end
}
